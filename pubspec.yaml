name: slash_task
description: "A new Flutter project."

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: '>=3.4.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  flutter_bloc: ^8.0.0 # State management using BLoC
  equatable: ^2.0.0    # To make state comparisons easier
  http: ^0.13.4        # To fetch data from JSON file
  freezed_annotation: ^2.0.0
  json_annotation: ^4.7.0
  get_it: ^7.7.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  build_runner: ^2.3.3
  freezed: ^2.3.5
  json_serializable: ^6.6.1

flutter:
  uses-material-design: true
  assets:
    - assets/data/
    - assets/images/
